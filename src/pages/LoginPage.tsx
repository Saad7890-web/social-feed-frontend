export default function LoginPage() {
  return (
    <main style={{ minHeight: "100vh", display: "grid", placeItems: "center" }}>
      <section>
        <h1>Login</h1>
        <p>Step 2 will add the full form and legacy design.</p>
      </section>
    </main>
  );
}
